import 'package:flutter/material.dart';

abstract class AppColors {
  /// The main deep purple background color (0xFF22124B).
  static const purple = Color(0xFF22124B);

  /// The primary teal accent color (0xFF1DD6C1).
  static const teal = Color(0xFF1DD6C1);

  /// The disabled version of the teal color (0x801DD6C1).
  static const tealDisabled = Color(0x801DD6C1);
}
